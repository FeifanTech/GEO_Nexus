# 🤖 AI 开发规范

> 适用于所有 AI 开发工具（Claude Code, GitHub Copilot, Cursor 等）

本文档定义了 AI 工具在 GEO Nexus 项目中的行为规范，确保代码质量和团队协作效率。

---

## 🚫 强制禁止操作

### 1. Git 工作流规范

#### ❌ 禁止直接提交 main 分支
**规则**: 任何代码更改都必须通过功能分支提交，再合并到 main 分支。

**正确流程**:
```bash
# 1. 创建功能分支
git checkout -b feat/feature-name

# 2. 在功能分支上开发和提交
git add .
git commit -m "feat: add new feature"

# 3. 合并到 main
git checkout main
git merge feat/feature-name --no-ff
```

**错误示例**:
```bash
# ❌ 直接在 main 分支提交
git checkout main
git add .
git commit -m "some changes"  # 禁止！
```

#### ❌ 禁止使用快进合并（Fast-forward merge）
**规则**: 合并分支时必须使用 `--no-ff` 选项，保留分支历史。

```bash
# ✅ 正确
git merge feat/feature-name --no-ff

# ❌ 错误
git merge feat/feature-name  # 会使用快进合并
```

### 2. 代码修改规范

#### ❌ 禁止在未读取文件的情况下修改代码
**规则**: 修改任何文件前，必须先使用 Read 工具读取文件内容，理解现有代码后再修改。

**正确流程**:
```
1. 使用 Read 工具读取目标文件
2. 分析现有代码结构和逻辑
3. 使用 Edit 或 Write 工具修改代码
```

**错误示例**:
```
❌ 直接使用 Write 工具覆盖文件，未先读取
```

#### ❌ 禁止过度工程化
**规则**: 只修改与任务直接相关的代码，不添加未要求的功能。

**示例**:
- ❌ 用户要求修复 bug，但添加了新功能
- ❌ 用户要求添加按钮，但重构了整个组件
- ✅ 用户要求修复 bug，只修复 bug
- ✅ 用户要求添加按钮，只添加按钮

---

## ✅ 必须遵守的规则

### 1. 提交信息规范

#### 格式要求
遵循 **Conventional Commits** 规范:

```
<type>(<scope>): <subject>

[optional body]

[optional footer]

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>
```

#### Type 类型
| Type | 说明 | 示例 |
|------|------|------|
| `feat` | 新功能 | `feat(diagnosis): 添加 GEO 排名检查功能` |
| `fix` | 修复 bug | `fix(api): 修复 Dify API 证书错误` |
| `docs` | 文档更新 | `docs(readme): 更新项目文档` |
| `style` | 代码格式（不影响功能） | `style: 格式化代码` |
| `refactor` | 重构 | `refactor(auth): 重构认证逻辑` |
| `perf` | 性能优化 | `perf(list): 优化产品列表渲染` |
| `test` | 测试 | `test(api): 添加 API 单元测试` |
| `chore` | 构建/工具链更新 | `chore: 更新依赖版本` |

#### 完整示例
```bash
git commit -m "$(cat <<'EOF'
feat(diagnosis): 添加 GEO 排名诊断功能

实现了以下功能：
- Dify API 集成
- 流式响应处理
- 错误处理机制

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>
EOF
)"
```

### 2. 代码风格规范

#### TypeScript 规范
- ✅ 使用严格类型检查（`strict: true`）
- ✅ 避免使用 `any` 类型
- ✅ 为所有函数参数和返回值添加类型
- ✅ 使用接口（`interface`）定义复杂对象

**示例**:
```typescript
// ✅ 正确
interface User {
  id: string;
  name: string;
  email: string;
}

function getUser(id: string): Promise<User> {
  // ...
}

// ❌ 错误
function getUser(id: any): any {
  // ...
}
```

#### 路径别名规范
- ✅ 使用 `@/*` 路径别名引用 src 目录
- ❌ 不使用相对路径 `../../`

**示例**:
```typescript
// ✅ 正确
import { Button } from "@/components/ui/button";
import { useProductStore } from "@/store/useProductStore";

// ❌ 错误
import { Button } from "../../components/ui/button";
import { useProductStore } from "../../../store/useProductStore";
```

#### 命名规范
| 类型 | 命名规范 | 示例 |
|------|----------|------|
| 组件 | PascalCase | `ProductList`, `DiagnosisPanel` |
| 函数 | camelCase | `fetchProducts`, `handleClick` |
| 常量 | UPPER_SNAKE_CASE | `API_BASE_URL`, `MAX_RETRIES` |
| 接口 | PascalCase | `User`, `ProductData` |
| 类型 | PascalCase | `TaskType`, `ResponseMode` |

### 3. 文件组织规范

#### 组件结构
```typescript
// 1. Imports - 按顺序分组
import React from "react";              // React 相关
import { useRouter } from "next/router"; // Next.js 相关
import { Button } from "@/components/ui"; // UI 组件
import { useProductStore } from "@/store"; // Store
import type { Product } from "@/types";  // 类型定义

// 2. 类型定义
interface Props {
  product: Product;
  onSelect?: (id: string) => void;
}

// 3. 组件定义
export function ProductCard({ product, onSelect }: Props) {
  // 组件逻辑
}
```

#### 文件位置规范
| 类型 | 位置 | 示例 |
|------|------|------|
| 页面组件 | `src/app/<page>/page.tsx` | `src/app/diagnosis/page.tsx` |
| 复用组件 | `src/components/` | `src/components/ProductCard.tsx` |
| UI 组件 | `src/components/ui/` | `src/components/ui/button.tsx` |
| Hooks | `src/hooks/` | `src/hooks/useProducts.ts` |
| Store | `src/store/` | `src/store/useProductStore.ts` |
| 工具函数 | `src/lib/` | `src/lib/utils.ts` |
| 类型定义 | `src/types/` | `src/types/product.ts` |

---

## 📋 开发检查清单

在每次代码修改后，检查以下项目：

### 提交前检查
- [ ] 已创建功能分支（`feat/*`, `fix/*`, `docs/*` 等）
- [ ] 所有修改的文件都已读取过
- [ ] 代码遵循 TypeScript 类型规范
- [ ] 使用了 `@/*` 路径别名
- [ ] 提交信息遵循 Conventional Commits
- [ ] 添加了 `Co-Authored-By` 标签
- [ ] 使用 `--no-ff` 合并到 main

### 代码质量检查
- [ ] ESLint 无错误
- [ ] TypeScript 类型检查通过
- [ ] Prettier 格式化完成
- [ ] 无 console.log 调试代码（除非必要）
- [ ] 无未使用的导入和变量

### 功能检查
- [ ] 只修改了必要的代码
- [ ] 未添加未要求的功能
- [ ] 保持简单直接，避免过度设计
- [ ] 遵循现有代码风格和模式

---

## 🎯 AI 工具特定规范

### Claude Code
- ✅ 使用 Read 工具读取文件
- ✅ 使用 Edit 工具修改现有文件
- ✅ 使用 Write 工具创建新文件
- ✅ 使用 Bash 工具执行 Git 命令
- ❌ 不使用 `sed`/`awk` 等命令修改文件

### GitHub Copilot / Cursor
- ✅ 接受建议前检查类型定义
- ✅ 确保路径别名正确
- ✅ 遵循项目命名规范
- ❌ 不盲目接受所有建议

---

## 🔍 常见错误示例

### 错误 1: 直接提交 main 分支
```bash
# ❌ 错误
git checkout main
git add .
git commit -m "fix: some bug"

# ✅ 正确
git checkout -b fix/some-bug
git add .
git commit -m "fix: some bug"
git checkout main
git merge fix/some-bug --no-ff
```

### 错误 2: 提交信息不规范
```bash
# ❌ 错误
git commit -m "updated files"
git commit -m "fix bug"
git commit -m "add feature"

# ✅ 正确
git commit -m "fix(api): 修复 Dify API 证书错误"
git commit -m "feat(diagnosis): 添加 GEO 排名检查功能"
```

### 错误 3: 相对路径导入
```typescript
// ❌ 错误
import { Button } from "../../../components/ui/button";

// ✅ 正确
import { Button } from "@/components/ui/button";
```

### 错误 4: 缺少类型定义
```typescript
// ❌ 错误
function fetchData(id) {
  return fetch(\`/api/products/\${id}\`);
}

// ✅ 正确
async function fetchData(id: string): Promise<Product> {
  const response = await fetch(\`/api/products/\${id}\`);
  return response.json();
}
```

---

## 📚 相关文档

- [CONTRIBUTING.md](./CONTRIBUTING.md) - 开发贡献指南
- [README.md](./README.md) - 项目文档
- [ARCHITECTURE.md](./ARCHITECTURE.md) - 架构文档
- [Conventional Commits](https://www.conventionalcommits.org/) - 提交信息规范

---

## 🆘 需要帮助？

如果对开发规范有疑问：

1. 查看 [CONTRIBUTING.md](./CONTRIBUTING.md) 获取详细指南
2. 参考现有代码的模式和风格
3. 咨询项目维护者

---

*AI 开发规范 - GEO Nexus Platform*
*最后更新: 2026-01-29*

✨ **遵循这些规范，让 AI 成为高效的开发伙伴！**
